# enables profiling
#zmodload zsh/zprof

# Lines configured by zsh-newuser-install
HISTFILE=~/.histfile
HISTSIZE=10000
SAVEHIST=10000
unsetopt beep
bindkey -v

# eval "$(zoxide init zsh)"

alias cd="z"
alias ns="nix-shell"
